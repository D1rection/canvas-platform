/**
 * 渲染层统一导出
 *
 * 结构：
 * - elements/  元素渲染组件（RectShape, CircleShape 等）
 * - overlay/   覆盖层组件（SelectionBox, HoverOverlay 等）
 */

// 元素渲染
export * from "./elements";

// 覆盖层
export * from "./overlay";
